pkgdatadir = join_paths(get_option('prefix'), get_option('datadir'), meson.project_name())
viewsdir = join_paths(pkgdatadir, 'kabegami/views')

kabegami_sources = [
  '__init__.py',
  'not_implemented.py',
]

install_data(kabegami_sources, install_dir: viewsdir)