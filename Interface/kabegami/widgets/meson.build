pkgdatadir = join_paths(get_option('prefix'), get_option('datadir'), meson.project_name())
widgetsdir = join_paths(pkgdatadir, 'kabegami/widgets')

kabegami_sources = [
  '__init__.py',
  'theme_switcher.py',
]

install_data(kabegami_sources, install_dir: widgetsdir)